# bunq-go SDK

## Generated files

**NEVER edit `*_gen.go` files directly.** These are generated by `cmd/generate/main.go` from the Python SDK source. To fix types or add fields, update the generator and re-run:

```sh
go run ./cmd/generate
```
